using UnityEngine;

public class Intelligence : Stat
{
    public Intelligence(StatType type, int amount) : base(type, amount)
    {
        
    }

    // Start is called before the first frame update
    void Awake()
    {
    }
}
