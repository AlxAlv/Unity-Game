using UnityEngine;

public class Strength : Stat
{
    public Strength(StatType type, int amount) : base(type, amount)
    {

    }

    // Start is called before the first frame update
    void Awake()
    {
    }
}
