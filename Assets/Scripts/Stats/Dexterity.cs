using UnityEngine;

public class Dexterity : Stat
{
    public Dexterity(StatType type, int amount) : base(type, amount)
    {

    }

    // Start is called before the first frame update
    void Awake()
    {
    }
}
